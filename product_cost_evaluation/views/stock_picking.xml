<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_picking_form_inherit" model="ir.ui.view">
        <field name="name">view.picking.form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='move_lines']/tree/field[@name='product_uom']" position="after">
                <field name="company_currency_id" invisible="1"/>
                <field name="manual_cost" widget="monetary" readonly="1"/>
                <button name="button_edit_manual_cost"
                        type="object"
                        context="{'original_manual_cost': manual_cost}"
                        icon="fa-pencil-square-o"
                        string="Edit Manual Cost"/>
                <field name="manual_cost_total" widget="monetary" invisible="1"/>
                <field name="purchase_cost" widget="monetary" readonly="1"/>
                <field name="purchase_cost_total" widget="monetary" invisible="1"/>
                <field name="invoice_cost" widget="monetary" readonly="1"/>
                <field name="invoice_cost_total" widget="monetary" invisible="1"/>
            </xpath>
        </field>
    </record>

</odoo>
